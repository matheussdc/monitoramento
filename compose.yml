services:
  app:
    build: .
    container_name: teste-c
    volumes:
      - ./database:/app/database
    ports:
      - "4567:4567"

  # prometheus:
  #   image: prom/prometheus
  #   container_name: prometheus-cont
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - "./prometheus.yml:/etc/prometheus/prometheus.yml"

  # grafana:
  #   image: grafana/grafana-enterprise
  #   container_name: grafana-cont
  #   ports:
  #     - "3000:3000"
